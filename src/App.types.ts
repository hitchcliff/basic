export enum Direction {
  Home = "/",
  Auth = "/auth",
  Projects = "/projects",
  Blog = "/blog",
  Dashboard = "/dashboard",
  SinglePost = "blog/:postid/",
  SingleProject = "projects/:projectid",
}
